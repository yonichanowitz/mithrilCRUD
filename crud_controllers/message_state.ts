import MessageState from './customTypes/message_type'

const CurrMessageState: MessageState = {
    messages: [],
    inEditString: "",
    single: false,
}
// {id:1, body: 'the dudest dude of all dudes'}

export default CurrMessageState